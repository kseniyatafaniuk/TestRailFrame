package pages;

import baseEntitites.BasePage;
import core.BrowserService;

public class TestPage extends BasePage {

    public TestPage(BrowserService browserService) {
        super(browserService, "at");
    }

}
